#include "Sphere.h"

/*
void Sphere::generateSphere()
{

	vertice vert;
	ico.generate(0.002,3);

	mesh.vertex.index = ico.indiceIc;
	mesh.vertex.vertices = ico.verticeIc;

	vert.vertices = mesh.vertex.vertices;
	vert.index = mesh.vertex.index;

	mesh.generateVao(vert);
}

void Sphere::render(Renderer* renderer, ResourceManager* rm)
{
	renderer->setVao(mesh.vao);
	renderer->setShader(rm->getShader("default"));
	renderer->setIndexedDrawing(mesh.numOfIndexElements);
	renderer->render();

}

*/

void Sphere::render()
{

}
